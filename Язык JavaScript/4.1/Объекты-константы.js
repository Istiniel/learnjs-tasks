function alert(...args) {
  console.log(...args);
}

// Поля объекта объявленного как const допускается изменять. const в случае объявления объекта ссылается непосредственно на объект, а не конкретного на его поля
